# 🎯 Настройка целей Яндекс Метрики и интеграция с RealtyCalendar

## Обзор

Данная документация описывает оптимизированную систему целей для Яндекс Директа, интегрированную с системой бронирования RealtyCalendar.

**Счётчик Яндекс Метрики:** `103308092`

---

## 📊 Оптимизированные цели для Директа

### Иерархия целей (4 основных)

| Приоритет | Цель | Идентификатор | Тип | Ценность |
|-----------|------|---------------|-----|----------|
| 🥇 1 | **Завершённое бронирование** | `booking_complete` | JavaScript-событие + Webhook | Максимальная |
| 🥈 2 | Отправка формы связи | `contact_form_submit` | JavaScript-событие | Высокая |
| 🥉 3 | Клик на телефон | `phone_click` | JavaScript-событие | Средняя |
| 4 | Клик на мессенджер | `messenger_click` | JavaScript-событие | Средняя |

---

## 🔧 Настройка в Яндекс Метрике

### Шаг 1: Создание целей

1. Перейдите в [Яндекс Метрику](https://metrika.yandex.ru/)
2. Выберите счётчик `103308092`
3. Перейдите в **Настройки** → **Цели**
4. Создайте следующие цели:

#### Цель 1: Бронирование (ГЛАВНАЯ)
- **Название:** `Завершённое бронирование`
- **Тип:** JavaScript-событие
- **Идентификатор:** `booking_complete`
- **Ценность цели:** Указать среднюю стоимость бронирования (например, 15000 RUB)

#### Цель 2: Форма связи
- **Название:** `Отправка формы связи`
- **Тип:** JavaScript-событие
- **Идентификатор:** `contact_form_submit`
- **Ценность цели:** ~2000 RUB (оценочная конверсия заявки в бронирование)

#### Цель 3: Звонок
- **Название:** `Клик на телефон`
- **Тип:** JavaScript-событие
- **Идентификатор:** `phone_click`
- **Ценность цели:** ~3000 RUB

#### Цель 4: Мессенджер
- **Название:** `Переход в мессенджер`
- **Тип:** JavaScript-событие
- **Идентификатор:** `messenger_click`
- **Ценность цели:** ~2500 RUB

### Шаг 2: Включение офлайн-конверсий

1. Перейдите в **Настройки** → **Загрузка данных**
2. Включите **Учёт офлайн-конверсий**
3. Установите период: **21 день** (максимум для Метрики)

### Шаг 3: Настройка интеграции с Директом

1. Перейдите в **Настройки** → **Цели**
2. Включите опцию: **Разрешить в рекламных кампаниях оптимизацию по целям**
3. В Яндекс Директ привяжите счётчик к кампаниям

---

## 🔗 Настройка Webhook в RealtyCalendar

### URL для Webhook

```
https://lavillapine.onrender.com/api/webhook/realtycalendar
```

### Инструкция настройки

1. Войдите в [RealtyCalendar](https://realtycalendar.ru/)
2. Перейдите в **Настройки** → **Интеграции** → **Webhook**
3. Добавьте новый Webhook:
   - **URL:** `https://lavillapine.onrender.com/api/webhook/realtycalendar`
   - **События для отслеживания:**
     - ✅ `reservation_created` - Новое бронирование
     - ✅ `payment_received` - Получена оплата
     - ☑️ `reservation_cancelled` - Отмена (для аналитики)
4. Сохраните настройки

### Проверка работы Webhook

Тестовый endpoint:
```
GET https://lavillapine.onrender.com/api/webhook/test
```

Ответ:
```json
{
  "success": true,
  "message": "Webhook endpoint работает",
  "webhookUrl": "https://lavillapine.onrender.com/api/webhook/realtycalendar",
  "instructions": {
    "events": ["reservation_created", "payment_received", "reservation_cancelled"],
    "goals": {
      "booking_complete": "Главная цель - завершённое бронирование"
    }
  }
}
```

---

## 📁 Структура файлов

```
client/
├── index.html                      # Улучшенная конфигурация счётчика
└── src/
    └── lib/
        └── yandex-metrika.ts       # Утилиты для работы с Метрикой

server/
├── routes.ts                       # Webhook endpoints
└── realtycalendar-webhook.ts       # Обработчик webhook
```

---

## 🔄 Отслеживаемые события

### На клиенте (автоматически)

| Компонент | Событие | Цель |
|-----------|---------|------|
| `contact-form.tsx` | Успешная отправка формы | `contact_form_submit` |
| `contact-buttons.tsx` | Клик на телефон | `phone_click` |
| `contact-buttons.tsx` | Клик на WhatsApp | `messenger_click` |
| `contact-buttons.tsx` | Клик на Telegram | `messenger_click` |
| `header.tsx` | Клик на телефон | `phone_click` |
| `header.tsx` | Клик на Telegram | `messenger_click` |

### На сервере (через Webhook)

| Событие RealtyCalendar | Цель | Действие |
|------------------------|------|----------|
| `reservation_created` | `booking_complete` | Логирование для офлайн-конверсий |
| `payment_received` | `booking_complete` | Логирование с суммой оплаты |
| `reservation_cancelled` | - | Только аналитика |

---

## 📈 API Endpoints

### Webhook от RealtyCalendar
```
POST /api/webhook/realtycalendar
Content-Type: application/json

{
  "event": "reservation_created",
  "timestamp": "2026-01-09T12:00:00Z",
  "data": {
    "reservation_id": "12345",
    "guest_name": "Иван Петров",
    "guest_phone": "+79189240007",
    "total_price": 15000,
    "check_in": "2026-01-15",
    "check_out": "2026-01-17"
  }
}
```

### Логи бронирований
```
GET /api/webhook/bookings?limit=100
```

### Экспорт CSV для Метрики
```
GET /api/webhook/conversions/csv
```

Можно выбрать тип идентификатора (если вы передаёте его в webhook):
```
GET /api/webhook/conversions/csv?idType=ClientId
GET /api/webhook/conversions/csv?idType=yclid
```

Формат CSV для загрузки офлайн-конверсий:
```csv
ClientId,Target,DateTime,Price,Currency
133591247640966458,booking_complete,1767960000,15000,RUB
```

Примечания по формату (важно):
- Первый столбец — один из идентификаторов: `ClientId` или `yclid` (или другие поддерживаемые Метрикой), обязателен хотя бы один.
- `DateTime` — Unix time stamp (секунды), прошедшее время.

---

## 🎯 Оптимизация рекламных кампаний в Директе

### Рекомендуемые стратегии

1. **Оптимизация по главной цели** (`booking_complete`)
   - Установите эту цель как основную в стратегии
   - CPA: рассчитайте исходя из среднего чека и маржи

2. **Микроконверсии для обучения**
   - `contact_form_submit`, `phone_click`, `messenger_click`
   - Используйте для сбора данных на этапе обучения кампании

3. **Ретаргетинг**
   - Создайте аудитории на основе целей
   - Исключайте тех, кто уже забронировал

### Что НЕ использовать как цель

❌ Просмотр страницы галереи
❌ Скролл страницы
❌ Время на сайте
❌ Клик по социальным сетям (Instagram, VK)

Эти метрики "сливают бюджет", так как не коррелируют напрямую с бронированиями.

---

## 🔧 Улучшения конфигурации счётчика

В `index.html` добавлены следующие опции:

```javascript
ym(103308092, "init", {
    clickmap: true,          // Карта кликов
    trackLinks: true,        // Отслеживание ссылок
    accurateTrackBounce: true, // Точный подсчёт отказов
    webvisor: true,          // Вебвизор (запись сессий)
    trackHash: true,         // Отслеживание хэшей в URL
    triggerEvent: true,      // Триггерные события
    ecommerce: "dataLayer"   // Электронная коммерция
});
```

### Автоматическое сохранение yclid

При переходе из Директа автоматически сохраняется `yclid` в `localStorage` для последующей привязки офлайн-конверсий.

---

## 📞 Поддержка

При возникновении вопросов по настройке:

1. Проверьте логи на сервере: `/api/webhook/bookings`
2. Тестируйте webhook: `/api/webhook/test`
3. Экспортируйте CSV: `/api/webhook/conversions/csv`

---

## 🚀 Следующие шаги

1. [ ] Создать цели в интерфейсе Яндекс Метрики
2. [ ] Настроить Webhook в RealtyCalendar
3. [ ] Привязать счётчик к кампаниям Директа
4. [ ] Настроить стратегию оптимизации по `booking_complete`
5. [ ] Опционально: Получить OAuth-токен для автоматической загрузки конверсий через API
